#import <Foundation/Foundation.h>
/// Phase 2.3: Single transformer layer training step on ANE
/// Proves: compile → forward → loss → backward → weight update → loss decreases
NSString *ane_train_step_test(void);
